[package]
name = "ape-cli"
description = "An executable laboratory for the Assertion Projection Engine."
readme = "README.md"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
ape = { path = "../core" }

[dev-dependencies]
ape = { path = "../core", features = ["conformance"] }
